server:
  port: 8080

spring:
  application:
    name: low-altitude-economy-agent

  ai:
    dashscope:
      api-key: sk-9d90ce59d9124aba8796996ae2b083f0
      chat:
        options:
          model: qwen3-max
          temperature: 0.5
          max-tokens: 15000

  datasource:
#    url: jdbc:h2:mem:lowaltitude;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
    url: jdbc:mysql://49.233.253.104:62036/metric_analyst_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: b4d@1qaz
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

#  h2:
#    console:
#      enabled: true
#      path: /h2-console

#  sql:
#    init:
#      schema-locations: classpath:db/schema-h2.sql
#      data-locations: classpath:db/data.sql
#      mode: always

logging:
  level:
    com.alibaba.cloud.ai: DEBUG
    com.lowaltitude.agent: DEBUG
    org.hibernate.SQL: DEBUG
